<%- include('layout',{body:`<section class="hero"><h1>اختر سيرفر ZEUS</h1><p>تظهر فقط السيرفرات التي تملك فيها Manage Guild ويوجد فيها البوت.</p></section><div class="grid cards">${guilds.map(g=>`<a class="card ${botGuilds.has(g.id)?'':'disabled'}" href="${botGuilds.has(g.id)?'/guild/'+g.id:'#'}"><b>${g.name}</b><span>${botGuilds.has(g.id)?'متاح':'البوت غير موجود'}</span></a>`).join('')}</div>`}) %>
